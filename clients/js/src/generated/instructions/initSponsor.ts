/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
	addDecoderSizePrefix,
	addEncoderSizePrefix,
	combineCodec,
	fixDecoderSize,
	fixEncoderSize,
	getAddressEncoder,
	getBytesDecoder,
	getBytesEncoder,
	getProgramDerivedAddress,
	getStructDecoder,
	getStructEncoder,
	getU32Decoder,
	getU32Encoder,
	getUtf8Decoder,
	getUtf8Encoder,
	transformEncoder,
	type AccountMeta,
	type AccountSignerMeta,
	type Address,
	type Codec,
	type Decoder,
	type Encoder,
	type Instruction,
	type InstructionWithAccounts,
	type InstructionWithData,
	type ReadonlyAccount,
	type ReadonlyUint8Array,
	type TransactionSigner,
	type WritableAccount,
	type WritableSignerAccount,
} from '@solana/kit';
import { PYTHIA_OP_PROGRAM_ADDRESS } from '../programs';
import { expectAddress, getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const INIT_SPONSOR_DISCRIMINATOR = new Uint8Array([171, 84, 165, 61, 98, 53, 242, 108]);

export function getInitSponsorDiscriminatorBytes() {
	return fixEncoderSize(getBytesEncoder(), 8).encode(INIT_SPONSOR_DISCRIMINATOR);
}

export type InitSponsorInstruction<
	TProgram extends string = typeof PYTHIA_OP_PROGRAM_ADDRESS,
	TAccountAuthority extends string | AccountMeta<string> = string,
	TAccountSponsor extends string | AccountMeta<string> = string,
	TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
	TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
	InstructionWithData<ReadonlyUint8Array> &
	InstructionWithAccounts<
		[
			TAccountAuthority extends string
				? WritableSignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
				: TAccountAuthority,
			TAccountSponsor extends string ? WritableAccount<TAccountSponsor> : TAccountSponsor,
			TAccountSystemProgram extends string
				? ReadonlyAccount<TAccountSystemProgram>
				: TAccountSystemProgram,
			...TRemainingAccounts,
		]
	>;

export type InitSponsorInstructionData = {
	discriminator: ReadonlyUint8Array;
	name: string;
};

export type InitSponsorInstructionDataArgs = { name: string };

export function getInitSponsorInstructionDataEncoder(): Encoder<InitSponsorInstructionDataArgs> {
	return transformEncoder(
		getStructEncoder([
			['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
			['name', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
		]),
		(value) => ({ ...value, discriminator: INIT_SPONSOR_DISCRIMINATOR }),
	);
}

export function getInitSponsorInstructionDataDecoder(): Decoder<InitSponsorInstructionData> {
	return getStructDecoder([
		['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
		['name', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
	]);
}

export function getInitSponsorInstructionDataCodec(): Codec<
	InitSponsorInstructionDataArgs,
	InitSponsorInstructionData
> {
	return combineCodec(
		getInitSponsorInstructionDataEncoder(),
		getInitSponsorInstructionDataDecoder(),
	);
}

export type InitSponsorAsyncInput<
	TAccountAuthority extends string = string,
	TAccountSponsor extends string = string,
	TAccountSystemProgram extends string = string,
> = {
	authority: TransactionSigner<TAccountAuthority>;
	sponsor?: Address<TAccountSponsor>;
	systemProgram?: Address<TAccountSystemProgram>;
	name: InitSponsorInstructionDataArgs['name'];
};

export async function getInitSponsorInstructionAsync<
	TAccountAuthority extends string,
	TAccountSponsor extends string,
	TAccountSystemProgram extends string,
	TProgramAddress extends Address = typeof PYTHIA_OP_PROGRAM_ADDRESS,
>(
	input: InitSponsorAsyncInput<TAccountAuthority, TAccountSponsor, TAccountSystemProgram>,
	config?: { programAddress?: TProgramAddress },
): Promise<
	InitSponsorInstruction<
		TProgramAddress,
		TAccountAuthority,
		TAccountSponsor,
		TAccountSystemProgram
	>
> {
	// Program address.
	const programAddress = config?.programAddress ?? PYTHIA_OP_PROGRAM_ADDRESS;

	// Original accounts.
	const originalAccounts = {
		authority: { value: input.authority ?? null, isWritable: true },
		sponsor: { value: input.sponsor ?? null, isWritable: true },
		systemProgram: { value: input.systemProgram ?? null, isWritable: false },
	};
	const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;

	// Original args.
	const args = { ...input };

	// Resolve default values.
	if (!accounts.sponsor.value) {
		accounts.sponsor.value = await getProgramDerivedAddress({
			programAddress,
			seeds: [
				getBytesEncoder().encode(new Uint8Array([115, 112, 111, 110, 115, 111, 114])),
				getAddressEncoder().encode(expectAddress(accounts.authority.value)),
			],
		});
	}
	if (!accounts.systemProgram.value) {
		accounts.systemProgram.value =
			'11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
	}

	const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
	return Object.freeze({
		accounts: [
			getAccountMeta(accounts.authority),
			getAccountMeta(accounts.sponsor),
			getAccountMeta(accounts.systemProgram),
		],
		data: getInitSponsorInstructionDataEncoder().encode(args as InitSponsorInstructionDataArgs),
		programAddress,
	} as InitSponsorInstruction<TProgramAddress, TAccountAuthority, TAccountSponsor, TAccountSystemProgram>);
}

export type InitSponsorInput<
	TAccountAuthority extends string = string,
	TAccountSponsor extends string = string,
	TAccountSystemProgram extends string = string,
> = {
	authority: TransactionSigner<TAccountAuthority>;
	sponsor: Address<TAccountSponsor>;
	systemProgram?: Address<TAccountSystemProgram>;
	name: InitSponsorInstructionDataArgs['name'];
};

export function getInitSponsorInstruction<
	TAccountAuthority extends string,
	TAccountSponsor extends string,
	TAccountSystemProgram extends string,
	TProgramAddress extends Address = typeof PYTHIA_OP_PROGRAM_ADDRESS,
>(
	input: InitSponsorInput<TAccountAuthority, TAccountSponsor, TAccountSystemProgram>,
	config?: { programAddress?: TProgramAddress },
): InitSponsorInstruction<
	TProgramAddress,
	TAccountAuthority,
	TAccountSponsor,
	TAccountSystemProgram
> {
	// Program address.
	const programAddress = config?.programAddress ?? PYTHIA_OP_PROGRAM_ADDRESS;

	// Original accounts.
	const originalAccounts = {
		authority: { value: input.authority ?? null, isWritable: true },
		sponsor: { value: input.sponsor ?? null, isWritable: true },
		systemProgram: { value: input.systemProgram ?? null, isWritable: false },
	};
	const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;

	// Original args.
	const args = { ...input };

	// Resolve default values.
	if (!accounts.systemProgram.value) {
		accounts.systemProgram.value =
			'11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
	}

	const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
	return Object.freeze({
		accounts: [
			getAccountMeta(accounts.authority),
			getAccountMeta(accounts.sponsor),
			getAccountMeta(accounts.systemProgram),
		],
		data: getInitSponsorInstructionDataEncoder().encode(args as InitSponsorInstructionDataArgs),
		programAddress,
	} as InitSponsorInstruction<TProgramAddress, TAccountAuthority, TAccountSponsor, TAccountSystemProgram>);
}

export type ParsedInitSponsorInstruction<
	TProgram extends string = typeof PYTHIA_OP_PROGRAM_ADDRESS,
	TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
	programAddress: Address<TProgram>;
	accounts: {
		authority: TAccountMetas[0];
		sponsor: TAccountMetas[1];
		systemProgram: TAccountMetas[2];
	};
	data: InitSponsorInstructionData;
};

export function parseInitSponsorInstruction<
	TProgram extends string,
	TAccountMetas extends readonly AccountMeta[],
>(
	instruction: Instruction<TProgram> &
		InstructionWithAccounts<TAccountMetas> &
		InstructionWithData<ReadonlyUint8Array>,
): ParsedInitSponsorInstruction<TProgram, TAccountMetas> {
	if (instruction.accounts.length < 3) {
		// TODO: Coded error.
		throw new Error('Not enough accounts');
	}
	let accountIndex = 0;
	const getNextAccount = () => {
		const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
		accountIndex += 1;
		return accountMeta;
	};
	return {
		programAddress: instruction.programAddress,
		accounts: {
			authority: getNextAccount(),
			sponsor: getNextAccount(),
			systemProgram: getNextAccount(),
		},
		data: getInitSponsorInstructionDataDecoder().decode(instruction.data),
	};
}
