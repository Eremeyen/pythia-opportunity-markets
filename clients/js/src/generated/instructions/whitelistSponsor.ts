/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
	combineCodec,
	fixDecoderSize,
	fixEncoderSize,
	getBytesDecoder,
	getBytesEncoder,
	getStructDecoder,
	getStructEncoder,
	transformEncoder,
	type AccountMeta,
	type AccountSignerMeta,
	type Address,
	type FixedSizeCodec,
	type FixedSizeDecoder,
	type FixedSizeEncoder,
	type Instruction,
	type InstructionWithAccounts,
	type InstructionWithData,
	type ReadonlyUint8Array,
	type TransactionSigner,
	type WritableAccount,
	type WritableSignerAccount,
} from '@solana/kit';
import { PYTHIA_OP_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const WHITELIST_SPONSOR_DISCRIMINATOR = new Uint8Array([
	147, 216, 206, 186, 143, 127, 202, 82,
]);

export function getWhitelistSponsorDiscriminatorBytes() {
	return fixEncoderSize(getBytesEncoder(), 8).encode(WHITELIST_SPONSOR_DISCRIMINATOR);
}

export type WhitelistSponsorInstruction<
	TProgram extends string = typeof PYTHIA_OP_PROGRAM_ADDRESS,
	TAccountAdmin extends string | AccountMeta<string> = string,
	TAccountSponsor extends string | AccountMeta<string> = string,
	TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
	InstructionWithData<ReadonlyUint8Array> &
	InstructionWithAccounts<
		[
			TAccountAdmin extends string
				? WritableSignerAccount<TAccountAdmin> & AccountSignerMeta<TAccountAdmin>
				: TAccountAdmin,
			TAccountSponsor extends string ? WritableAccount<TAccountSponsor> : TAccountSponsor,
			...TRemainingAccounts,
		]
	>;

export type WhitelistSponsorInstructionData = {
	discriminator: ReadonlyUint8Array;
};

export type WhitelistSponsorInstructionDataArgs = {};

export function getWhitelistSponsorInstructionDataEncoder(): FixedSizeEncoder<WhitelistSponsorInstructionDataArgs> {
	return transformEncoder(
		getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
		(value) => ({ ...value, discriminator: WHITELIST_SPONSOR_DISCRIMINATOR }),
	);
}

export function getWhitelistSponsorInstructionDataDecoder(): FixedSizeDecoder<WhitelistSponsorInstructionData> {
	return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getWhitelistSponsorInstructionDataCodec(): FixedSizeCodec<
	WhitelistSponsorInstructionDataArgs,
	WhitelistSponsorInstructionData
> {
	return combineCodec(
		getWhitelistSponsorInstructionDataEncoder(),
		getWhitelistSponsorInstructionDataDecoder(),
	);
}

export type WhitelistSponsorInput<
	TAccountAdmin extends string = string,
	TAccountSponsor extends string = string,
> = {
	admin: TransactionSigner<TAccountAdmin>;
	sponsor: Address<TAccountSponsor>;
};

export function getWhitelistSponsorInstruction<
	TAccountAdmin extends string,
	TAccountSponsor extends string,
	TProgramAddress extends Address = typeof PYTHIA_OP_PROGRAM_ADDRESS,
>(
	input: WhitelistSponsorInput<TAccountAdmin, TAccountSponsor>,
	config?: { programAddress?: TProgramAddress },
): WhitelistSponsorInstruction<TProgramAddress, TAccountAdmin, TAccountSponsor> {
	// Program address.
	const programAddress = config?.programAddress ?? PYTHIA_OP_PROGRAM_ADDRESS;

	// Original accounts.
	const originalAccounts = {
		admin: { value: input.admin ?? null, isWritable: true },
		sponsor: { value: input.sponsor ?? null, isWritable: true },
	};
	const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;

	const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
	return Object.freeze({
		accounts: [getAccountMeta(accounts.admin), getAccountMeta(accounts.sponsor)],
		data: getWhitelistSponsorInstructionDataEncoder().encode({}),
		programAddress,
	} as WhitelistSponsorInstruction<TProgramAddress, TAccountAdmin, TAccountSponsor>);
}

export type ParsedWhitelistSponsorInstruction<
	TProgram extends string = typeof PYTHIA_OP_PROGRAM_ADDRESS,
	TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
	programAddress: Address<TProgram>;
	accounts: {
		admin: TAccountMetas[0];
		sponsor: TAccountMetas[1];
	};
	data: WhitelistSponsorInstructionData;
};

export function parseWhitelistSponsorInstruction<
	TProgram extends string,
	TAccountMetas extends readonly AccountMeta[],
>(
	instruction: Instruction<TProgram> &
		InstructionWithAccounts<TAccountMetas> &
		InstructionWithData<ReadonlyUint8Array>,
): ParsedWhitelistSponsorInstruction<TProgram, TAccountMetas> {
	if (instruction.accounts.length < 2) {
		// TODO: Coded error.
		throw new Error('Not enough accounts');
	}
	let accountIndex = 0;
	const getNextAccount = () => {
		const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
		accountIndex += 1;
		return accountMeta;
	};
	return {
		programAddress: instruction.programAddress,
		accounts: { admin: getNextAccount(), sponsor: getNextAccount() },
		data: getWhitelistSponsorInstructionDataDecoder().decode(instruction.data),
	};
}
