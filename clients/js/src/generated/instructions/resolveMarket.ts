/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
	combineCodec,
	fixDecoderSize,
	fixEncoderSize,
	getBooleanDecoder,
	getBooleanEncoder,
	getBytesDecoder,
	getBytesEncoder,
	getStructDecoder,
	getStructEncoder,
	transformEncoder,
	type AccountMeta,
	type AccountSignerMeta,
	type Address,
	type FixedSizeCodec,
	type FixedSizeDecoder,
	type FixedSizeEncoder,
	type Instruction,
	type InstructionWithAccounts,
	type InstructionWithData,
	type ReadonlyUint8Array,
	type TransactionSigner,
	type WritableAccount,
	type WritableSignerAccount,
} from '@solana/kit';
import { PYTHIA_OP_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const RESOLVE_MARKET_DISCRIMINATOR = new Uint8Array([155, 23, 80, 173, 46, 74, 23, 239]);

export function getResolveMarketDiscriminatorBytes() {
	return fixEncoderSize(getBytesEncoder(), 8).encode(RESOLVE_MARKET_DISCRIMINATOR);
}

export type ResolveMarketInstruction<
	TProgram extends string = typeof PYTHIA_OP_PROGRAM_ADDRESS,
	TAccountAuthority extends string | AccountMeta<string> = string,
	TAccountMarket extends string | AccountMeta<string> = string,
	TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
	InstructionWithData<ReadonlyUint8Array> &
	InstructionWithAccounts<
		[
			TAccountAuthority extends string
				? WritableSignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
				: TAccountAuthority,
			TAccountMarket extends string ? WritableAccount<TAccountMarket> : TAccountMarket,
			...TRemainingAccounts,
		]
	>;

export type ResolveMarketInstructionData = {
	discriminator: ReadonlyUint8Array;
	outcome: boolean;
};

export type ResolveMarketInstructionDataArgs = { outcome: boolean };

export function getResolveMarketInstructionDataEncoder(): FixedSizeEncoder<ResolveMarketInstructionDataArgs> {
	return transformEncoder(
		getStructEncoder([
			['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
			['outcome', getBooleanEncoder()],
		]),
		(value) => ({ ...value, discriminator: RESOLVE_MARKET_DISCRIMINATOR }),
	);
}

export function getResolveMarketInstructionDataDecoder(): FixedSizeDecoder<ResolveMarketInstructionData> {
	return getStructDecoder([
		['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
		['outcome', getBooleanDecoder()],
	]);
}

export function getResolveMarketInstructionDataCodec(): FixedSizeCodec<
	ResolveMarketInstructionDataArgs,
	ResolveMarketInstructionData
> {
	return combineCodec(
		getResolveMarketInstructionDataEncoder(),
		getResolveMarketInstructionDataDecoder(),
	);
}

export type ResolveMarketInput<
	TAccountAuthority extends string = string,
	TAccountMarket extends string = string,
> = {
	authority: TransactionSigner<TAccountAuthority>;
	market: Address<TAccountMarket>;
	outcome: ResolveMarketInstructionDataArgs['outcome'];
};

export function getResolveMarketInstruction<
	TAccountAuthority extends string,
	TAccountMarket extends string,
	TProgramAddress extends Address = typeof PYTHIA_OP_PROGRAM_ADDRESS,
>(
	input: ResolveMarketInput<TAccountAuthority, TAccountMarket>,
	config?: { programAddress?: TProgramAddress },
): ResolveMarketInstruction<TProgramAddress, TAccountAuthority, TAccountMarket> {
	// Program address.
	const programAddress = config?.programAddress ?? PYTHIA_OP_PROGRAM_ADDRESS;

	// Original accounts.
	const originalAccounts = {
		authority: { value: input.authority ?? null, isWritable: true },
		market: { value: input.market ?? null, isWritable: true },
	};
	const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;

	// Original args.
	const args = { ...input };

	const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
	return Object.freeze({
		accounts: [getAccountMeta(accounts.authority), getAccountMeta(accounts.market)],
		data: getResolveMarketInstructionDataEncoder().encode(
			args as ResolveMarketInstructionDataArgs,
		),
		programAddress,
	} as ResolveMarketInstruction<TProgramAddress, TAccountAuthority, TAccountMarket>);
}

export type ParsedResolveMarketInstruction<
	TProgram extends string = typeof PYTHIA_OP_PROGRAM_ADDRESS,
	TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
	programAddress: Address<TProgram>;
	accounts: {
		authority: TAccountMetas[0];
		market: TAccountMetas[1];
	};
	data: ResolveMarketInstructionData;
};

export function parseResolveMarketInstruction<
	TProgram extends string,
	TAccountMetas extends readonly AccountMeta[],
>(
	instruction: Instruction<TProgram> &
		InstructionWithAccounts<TAccountMetas> &
		InstructionWithData<ReadonlyUint8Array>,
): ParsedResolveMarketInstruction<TProgram, TAccountMetas> {
	if (instruction.accounts.length < 2) {
		// TODO: Coded error.
		throw new Error('Not enough accounts');
	}
	let accountIndex = 0;
	const getNextAccount = () => {
		const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
		accountIndex += 1;
		return accountMeta;
	};
	return {
		programAddress: instruction.programAddress,
		accounts: { authority: getNextAccount(), market: getNextAccount() },
		data: getResolveMarketInstructionDataDecoder().decode(instruction.data),
	};
}
